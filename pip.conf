[global]
# Chainguard Libraries first (carries the +cgr.N backported builds);
# PyPI as fallback for everything else. Auth via ~/.netrc build secret.
index-url = https://libraries.cgr.dev/python/simple
extra-index-url = https://pypi.org/simple

[install]
timeout = 30
